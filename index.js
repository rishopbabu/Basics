/**
 * @format
 */

import {AppRegistry} from 'react-native';
import PageRefreshConponent from './Components/PageRefreshComponent';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => PageRefreshConponent);
