/**
 * @format
 */

import {AppRegistry} from 'react-native';
import StatusBarComponent from './Components/StatusBarComponent';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => StatusBarComponent);
